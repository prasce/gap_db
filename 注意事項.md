# gap_db 注意事項

最後更新：2026-09-25

這份文件記錄資料表結構中**還無法確認的欄位**，以及 WMS 拋檔中**需要特別注意的地方**。
欄位的完整說明也寫在資料庫的欄位註解 (COMMENT) 裡，在 pgAdmin 選取欄位即可看到。

依據的檔案（皆在 `doc/`，不放進 repo）：

| 檔案 | 用途 |
|---|---|
| `O850502D_3PLDC_PO_Guidelines.docx` | Gap X12 EDI 850 規格 |
| `O832502D_3PLDC_Guidelines.doc` | Gap X12 EDI 832 規格 |
| `850_PO_62028556_Active.txt` → `..._000084.rc` | 850 原始檔與正確的 WMS 收貨拋檔 |
| `832_Item_Add_G53_003.txt` → `.txt.im` | 832 原始檔與正確的 WMS 商品拋檔 |
| `01.WMSc_IB_ItemMaster_832(888)_PH_3M_v0.0.8.xlsx` | 3M 的 ItemMaster mapping spec（`item_master` 表的依據） |

---

## 一、還無法確認的欄位

目前**沒有 `.rc` 檔的欄位規格**（850 → RCPHDR/RCPDETL/RCPCTNDR 的 mapping spec）。
有名稱的欄位是拿範例值和 850 原始檔比對出來的；下列欄位仍需要向 WMS 端確認。

### gapwmc_850_header (RCPHDR)

| 欄位 | 目前判斷 | 待確認事項 |
|---|---|---|
| `f45_in_dc_date` | 850 `DTM*996` In DC Date | 範例 PO 的 `DTM*002` In Store Date 也是同一天 (2026-09-25)，無法確定是哪一個 |
| `f04_receipt_id_type`、`f05_receipt_type` | 範例值都是 `DPO` | 850 規格沒有這個值，應是 WMS 轉檔時加上的；可能有其他值 |
| `f02_interface_record_id`、`f03_receipt_id` | 範例值都等於 PO 號 | 是否永遠等於 PO 號 |
| 其餘 `fNN`（無名稱） | 範例為空值 | 用途不明，型別暫定 `VARCHAR(255)` |

### gapwmc_850_detail (RCPDETL)

| 欄位 | 目前判斷 | 待確認事項 |
|---|---|---|
| `f71_product_type` | 推測為 850 `PO113` Product Type（0 = Bulk, 1 = Pack） | 範例值都是 `0`，與 PO113 一致，但未證實 |
| 其餘 `fNN`（無名稱） | 範例為空值 | 用途不明 |

### gapwmc_850_carton (RCPCTNDR)

| 欄位 | 目前判斷 | 待確認事項 |
|---|---|---|
| `f05` | 範例值 `0` | 用途不明（可能是箱數或數量） |
| `f04`、`f06` | 範例為空值 | 用途不明 |

### gapwmc_832_item (.im)

| 欄位 | 待確認事項 |
|---|---|
| `length`、`width`、`height`、`weight`、`units_per_carton`、`units_per_pallet`、`bundle_items`、`product_remarks`、`country_of_origin` | 60 筆範例全部為空，不確定之後是否會有值、會放什麼 |

---

## 二、需要注意的地方

### 1. `.im` 的欄位名稱和實際內容不符

`.im` 第一行的欄位名稱有幾個和內容對不上。資料庫**照檔案原樣存**（欄位名稱沿用檔案標題），查詢時請看下表：

| 資料庫欄位（檔案欄名） | 實際內容 | 832 來源 |
|---|---|---|
| `item_size` (Item Size) | **顏色**，例 `RAINBOW-001` | LIN05 (BO) |
| `item_colour` (Item Colour) | **尺寸**，例 `6-0006` | LIN07 (IZ) |
| `division` (Division) | **Group Id**，例 `335` | REF\*6P |
| `barcode` (Barcode) | **海關 HS 碼**，例 `6203423500`；多數為空 | TC2\*J |
| `udf1` (UDF1) | **真正的 EAN-13 條碼** | LIN11 (EN) |

其他組合欄位：

- `long_description`：`商品編號最後一碼-衣物類型-吊牌類型`，例 `5-FLAT-0`（LIN03 最後一碼 + PKG\*35 + PKG\*67）
- `department`：`部門-類別-子類別-品牌`，例 `3354-0008-0001-0003`（REF\*DP + REF\*ACD + REF\*X2 + REF\*19）

### 2. 商品編號在 WMS 拋檔中被截成 8 碼

Gap 的商品編號是 9 碼，WMS 拋檔只放前 8 碼：

- `.im`：`sku` = 前 8 碼，最後一碼放在 `long_description` 開頭
- `.rc`：`f05_item_number` = 前 8 碼，最後一碼放在 `f26_item_last_digit`

要和 832 / 850 原始檔對照時，需要把兩段接起來，例如：

```sql
SELECT f05_item_number || f26_item_last_digit AS full_item FROM gapwmc_850_detail;
```

### 3. 日期欄位存的是原始字串

`.rc` 的日期不能直接轉成 `TIMESTAMP`，所以存成 `VARCHAR(14)`。格式有兩種：

- 14 碼 `YYYYMMDDHHMISS`，例 `20260925000000`（`f45_in_dc_date`）
- 8 碼 `YYYYMMDD`，例 `20260528`（`f46_po_creation_date`）

查詢時統一取前 8 碼轉換：

```sql
SELECT to_date(left(f45_in_dc_date, 8), 'YYYYMMDD') FROM gapwmc_850_header;
```

### 4. 訂單狀態的值比規格多

850 規格只列 `APPROVED`、`COMPLETED`，但範例檔還有 `ACTIVE`、`CANCELLED`。
`f32_order_status` 不要假設只有規格上的兩種值。

### 5. 早期的 `.rc` 範例格式不同，不能匯入

`doc/GAPWMC_850_20260924 2.rc.txt` 的 RCPHDR 是 63 欄、RCPDETL 是 74 欄，和正確拋檔（46 / 71 欄）不同，欄位位置也不一樣。
匯入程式會拒絕這種檔案（欄數不符）。如果之後收到的拋檔欄數又改變，需要先更新 `gap_db.sql` 的表格定義。

### 6. `item_master` 和 Gap 的 `.im` 不是同一種格式

`item_master`（278 欄）是依 3M 的 ItemMaster mapping spec 建立的 SCALE ITM 格式，**Gap 的 `.im` 檔不會寫進這張表**，而是寫進 `gapwmc_832_item`。
`item_master` 目前保留、沒有資料。

---

## 三、匯入程式

程式位置：`scripts/import.mjs`。連線設定讀取專案根目錄的 `.env`。

```bash
pnpm run import-wms <檔案...>               # 匯入
pnpm run import-wms --dry-run <檔案...>     # 只檢查，不寫入
pnpm run import-wms --replace <檔案...>     # 同檔名已匯入過時，先刪除舊資料再匯入
```

- 依檔案第一行判斷類型：`Customer_Code|...` 為 `.im`，`RCPHDR|...` 為 `.rc`
- 每個檔案各自一個交易；任何一行出錯（欄數不符、型別錯誤等），整個檔案都不會寫入
- 以檔名（`source_file`）判斷是否重複匯入；已匯入過的檔案預設略過
- 欄位順序讀取自資料庫的表格定義，所以修改 `gap_db.sql` 的表格後，程式不用跟著改
